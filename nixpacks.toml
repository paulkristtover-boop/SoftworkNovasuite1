[phases.setup]
nixPkgs = ["nodejs_20"]

[phases.install]
cmds = [
  "npm install --prefix bot --omit=dev",
  "npm install --prefix database --omit=dev"
]

[phases.build]
cmds = ["echo 'NovaSuite bot – no build step'"]

[start]
cmd = "node database/migrate.js && node bot/bot.js"
