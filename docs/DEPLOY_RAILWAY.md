# Railway (bot)

1. Root Directory: empty (NovaSuite root)
2. Start: `node scripts/migrate.js && node bot.js`
3. Env: `BOT_TOKEN`, `ADMIN_IDS`, `DATABASE_URL`, `ADMIN_CMS_URL`, `NODE_ENV=production`
4. Attach PostgreSQL plugin
