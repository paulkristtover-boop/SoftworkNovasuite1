# Not used

SoftworkNovaSuite uses **one Telegraf bot** (`user-bot/`) for:

- All user interactions
- Admin notifications

Admin management is only in the **Next.js CMS** (`admin-cms/` on Vercel).

Do not run a second bot.
